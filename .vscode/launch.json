{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "preLaunchTask": "Build",
            "program": "${workspaceFolder}/build/icloud-for-linux",
            "args": ["notes", "Notes"],
            "environment": [{"name":"TMPDIR","value":"${workspaceFolder}/build"}],
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "type": "cppdbg",
            "osx": {
                "type": "lldb"
            }
        }
    ]
}
